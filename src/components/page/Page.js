import React from 'react';
import PropTypes from 'prop-types';

Page.propTypes = {
    
};

function Page(props) {
    return (
        <div>
            <h1>page</h1>
        </div>
    );
}

export default Page;